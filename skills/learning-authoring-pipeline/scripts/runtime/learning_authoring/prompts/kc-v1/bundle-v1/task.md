# Task

Create one complete shared proposed KC set from the frozen ordered source bundle and optional
lecturer context.

Use `agent-read` to list the frozen sources and context, then read their source-qualified batches.
Retain each batch's capability inventory and unresolved evidence before moving on. Reconcile within
each source, then across sources; batch boundaries do not define KC boundaries. Merge only true
capability duplicates with source-qualified evidence. Inspect geometry-derived layout and images
where native text does not carry the relationship. Preserve conflicts and unresolved visuals rather
than inferring a familiar pattern.

Use worked examples only for provenance and split/merge patterns. All facts, counts, IDs, source
references, and language must come from the frozen input. Copy
`input_boundary.expected_source_ref` exactly and return only JSON matching the candidate schema.
