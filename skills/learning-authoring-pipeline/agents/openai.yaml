interface:
  display_name: "Learning Authoring Pipeline"
  short_description: "Author PDF slides into reviewable KC and Quiz"
  brand_color: "#1261A0"
  default_prompt: "Use $learning-authoring-pipeline to process the attached course PDF with this coding-agent subscription session and stop at each human review gate."

policy:
  allow_implicit_invocation: true
